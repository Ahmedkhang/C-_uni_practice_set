# Class # 5

1 Byte = 8 Bits


# Data Types

* char = 1 Byte
* int = 4 Bytes
* float =
* bool = 1 bit (because its only true false or on and off)

we can write int in a char data type but the number cant acceed from 255 or 256 because 1 byte has the memory limit
